#!/bin/sh
set -eu
script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
project_root=$(dirname -- "$script_dir")
export PYTHONPATH="$project_root/src${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m smacx_hermes "$@"
