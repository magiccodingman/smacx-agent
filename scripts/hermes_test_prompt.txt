Run a bounded semantic-only Alien Crossfire integration test. Call smac_new_game directly to start a new random single-player game on Citizen difficulty and Tiny world as faction 1, with blind research and narrative/tutorial UI disabled. Do not call smac_launch first; if a menu-only or running process already exists, stop it once and then call smac_new_game. Play through at least turn 6 using ONLY the SMACX MCP tools and the strict snapshot -> choices -> one guarded command -> fresh snapshot loop supplied by the harness system prompt.

Never use screenshots, vision, mouse, keyboard, computer-use, raw UI, or terminal commands to control the game. You may use the internet only to look up game rules or strategy if useful, but do not research rules you can infer from returned choices. Colony Pods found bases; Formers improve terrain. Use the fresh snapshot's ready_unit_refs to request unit_actions; never infer a replacement unit ID after production, founding, or combat. Make sensible basic choices: move a Colony Pod to a legal site and found a second base if possible, set production, explore, terraform if a Former becomes available, and manage research/economy. Keep deliberation concise and prioritize completing the bounded test: make at most one strategic base/economy adjustment per turn, then resolve ready units with a legal move/order or skip and end the turn.

If a required capability is absent or any unsupported modal appears, call smac_report_capability_gap exactly once and STOP rather than improvising; the current session will then mechanically reject further game commands. Record a few concise learned facts with smac_knowledge(action="put") using stable keys and the current match_id, session_id, and revision. Do not write match notes through a shell or arbitrary filesystem tool. At or after turn 6, create a semantic save slot named qwen_test, verify it is listed, stop the game, and summarize what you actually accomplished and any gaps.
Never put session-local unit, vehicle, base, or prototype IDs into durable knowledge; record stable names and facts without those shifting engine references.
