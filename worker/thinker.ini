[thinker]
# The worker starts Thinker in a private X server and controls the game only
# through the semantic bridge. Keep this file deliberately minimal so Thinker's
# compiled defaults remain authoritative while avoiding its fatal missing-file
# message box on a clean legal-copy import.
video_mode=2
DisableOpeningMovie=1
