# Optional temporal-graph projection. Core SMACX control and SQLite memory do not require it.
graphiti-core==0.29.3
# graphiti-core currently imports the original httpx/openai interfaces; prevent
# an unconstrained resolver from selecting the incompatible OpenAI 3.x line.
openai>=1.91.0,<2
httpx>=0.28.1,<1
