# PR 48 H1–H5 repair acceptance

## H4 — canonical orphan recovery

The journal validates the canonical chain under the existing root/timeline locks before returning an idempotent write, allocating a sequence, or replaying after restart. A unique contiguous valid suffix advances the manifest; corrupt hashes, scope mismatches, gaps, and competing suffixes fail explicitly without changing the manifest. Markers and indexes remain disposable. The unchanged own-writer fast path avoids rescanning prior payloads.

Acceptance: `journal_internal_crash_test.py` covers event-before-manifest with and without an idempotency key, manifest-before-marker, marker-before-catalog, S+2 linkage, and four invalid suffix adversaries. `campaign_journal_test.py` and `journal_idempotency_index_test.py` pass (zero canonical event reads for 100 new-key appends). `publication_transaction_test.py` passes 22 cases, including two failures inside real journal persistence followed by collector/store/journal recreation. Evidence is deterministic real-file journal persistence and native-shaped production collection, not running-game execution.

At this checkpoint, H1, H2/H3, H5 and integrated active-scope tail acceptance remained pending.

## H1 — provider publication visibility

Provider history is capped at the installed SQLite world-head observation cursor. World head and object loading share a read transaction. Runtime captures a projection once and uses its cursor for attention plus its objects for the anchor; changes queries and frozen specialist history use the same captured cap. History-derived recent-material promotion, journal event reads and optional Graphiti projection obey that visibility boundary. Attention stops before the first unpublished sequence, including later cursor-zero chat, so acknowledgment cannot skip the barrier.

`publication_visibility_test.py` pauses a production frozen publication concurrently before head installation and after head/before dependency capture, both normally and across crash/service recreation. Normal world changes, temporal reads, specialist snapshot, runtime and leases remain capped. After completion history appears once; attention is deliverable and acknowledged identities are not redelivered. All four cases pass. Runtime context and attention communication contracts pass. Synthetic attention fixtures now explicitly use installed cursors, retaining their delivery/ordering assertions. Evidence: native-shaped production collector with real concurrent readers and deterministic runtime/SQLite/journal tests.

At this checkpoint, H2/H3 and H5 remained pending; final integrated acceptance is recorded below.

## H2/H3 — feed-owned temporal episodes

A private episode checkpoint survives acknowledgment alongside the native source cursor. Each publication freezes its next checkpoint; acknowledgment installs both atomically, including post-head retry. Ordered feed gaps and identity resets terminate prior continuity. Retained observed movement receives a qualified new episode if no association is proved. The production projector and coalescer share per-occurrence assignments; projector movement duplication is disabled only when that production temporal authority is supplied.

A bounded one-row feed probe after snapshot collection does not drain later evidence. Only an empty complete probe at the same cut/action revision allows an open episode to join the endpoint snapshot. Otherwise the snapshot receives independent contact identity and later events remain for the next publication. Open temporal episodes imply stream continuity, never current whereabouts or durable opponent identity.

`cross_publication_episode_test.py`: 17 production collector/projector cases pass: cross-publication loss/destruction after service recreation; cut-before-snapshot loss/reappearance; retained appearance/no appearance × current/transient endpoint × normal/staged/frozen retry; page-boundary restart with two gaps and preserved pre-gap movement; four distinct episodes with retained post-reset movement. Existing six transient cases, native observation contracts and 22 publication transactions pass. Three batch-cache crash windows pass with raw cache durability assertions retained and provider invisibility added. Concurrent visibility rerun passes. Evidence is native-shaped production collection and deterministic persistence, not a live native-game comparison.

At this checkpoint, H5 and integrated/tail acceptance remained pending.

## H5 — inspection authority lifetime

Successful inspection metadata records an action-revision requirement only when the query carries that authority. Recent inspection selection preserves ordinary route/area/geography promotion across action-only churn; native receipt-backed paths retain their stronger lifetime. Cold and warm query results share this rule, including native expansion/counterfactual receipt authority. The authority follow-up below additionally validates unchanged dependencies across material revisions.

`inspection_action_lifetime_test.py`: eight cases (ordinary route, area, geography, native-receipt route × cold/warm) pass through actual query caching, anchor promotion, action-only head updates, restart, and material dependency invalidation. Derived lifecycle and full world-model contracts pass. A synthetic frontier-watch fixture now assigns its evidence to the installed publication, preserving its trigger/delivery assertions. Evidence is deterministic managed-query behavior with native-shaped receipts, not native mechanic equivalence.

Focused acceptance was complete; integrated contracts and final repeated active-scope acceptance are recorded below.

## H5 authority follow-up

Inspection promotion now calls the same complete dependency-set/hash validation used by issued derived queries, with the current ruleset and calculator version. It additionally checks any native action-revision receipt and scoped-area dependency. A bounded 32-candidate recent window avoids historical scans; automatic validation never renews the explicit inspection timestamp. Unrelated material changes no longer erase a still-valid inspection. Eight cold/warm cases now also cover unrelated material churn and restart. Derived lifecycle, history scaling, runtime/context and world-model contracts pass after this follow-up. The integrated suite and final timing distribution were rerun on this code, as recorded below.

## Integrated acceptance

All **41 distinct scripts** below pass. The integrated 39-script batch passed; the compiled adapter and five repetitions of the active-scope collector passed separately. After final journal schema/identity guards and replay locking, campaign journal, internal crash recovery, idempotency index and all 22 publication transaction cases were rerun successfully. The internal crash suite additionally proves concurrent replay waits for the writer lock and then recovers the installed orphan. LLVM/MinGW cross-build reports `ninja: no work to do`; native C++ is unchanged.

The internal canonical-journal matrix is:

| Case | Injection / adversary | Result |
| --- | --- | --- |
| A | Event installed, manifest absent; retry same key | Manifest advances, same sequence/event returned |
| B | Same window without key; recreated journal replay | Orphan reconciled before next append |
| C | Manifest installed, marker absent | Marker repaired, no duplicate |
| D | Marker installed, catalog/cache work absent | Canonical replay and next append valid |
| E | Distinct append after every recovered S+1 | Sequence S+2 links to recovered S+1 hash |
| F | Invalid hash | Fails closed, manifest unchanged |
| G | Wrong perspective/scope | Fails closed, manifest unchanged |
| H | Noncontiguous sequence | Fails closed, manifest unchanged |
| I | Two competing candidate suffixes | Fails closed, manifest unchanged |
| J | Missing marker, disposable index, external writer, restart, replay cache | Existing journal/index contracts pass; 100 new keys read zero historical event bodies |
| K | Collector failure inside event/manifest persistence | Both native-unchanged and native-reversed retries pass with recreated services |
| Additional | Invalid event schema, session identity, timestamp; concurrent reader | Invalid suffixes fail closed; replay waits for writer and recovers after crash |

### Five exact active-scope repetitions

Each fresh 320×160 parity map has 25,600 known squares, four proximity scopes, two geography scopes, one route corridor, two unions, and four hostile crossing watches. The real collector publishes an observed crossing followed by loss, yielding two crossing alerts in every run. Runs were sequential, without other heavy tests. The independent Python probe spans initial collection, scope creation and active publication; its legacy field name `UI_probe_max_gap_ms` does **not** mean a running-game native UI-thread measurement.

| Run | Initial collection ms | Scope creation ms | Active publication ms | Maximum probe gap ms |
| --- | ---: | ---: | ---: | ---: |
| 1 | 17633.365 | 40779.599 | 10421.472 | 402.815 |
| 2 | 8499.795 | 33734.419 | 11007.317 | 448.484 |
| 3 | 19147.112 | 34769.815 | 10779.913 | 406.654 |
| 4 | 8458.139 | 34560.980 | 10792.502 | 411.429 |
| 5 | 8474.952 | 35939.479 | 11013.907 | 426.970 |

Every run passes the unchanged 30,000 ms active-publication and 500 ms probe gates. Observed active publication min/median/max: 10421.472/10792.502/11013.907 ms. Observed probe gap min/median/max: 402.815/411.429/448.484 ms. Five samples describe this run distribution, not a population-tail or live-game guarantee. Both five-run H-review distributions are preserved, including the earlier 23.113-second initial collection. Scope setup costs remain visible separately from active publication. All prior failed and passing timing data remain unchanged, including the unexplained 55.9-second quiet outlier and the earlier 479.441 ms active-scope probe.

Context remains bounded: 15 tools, world schema 619 conservative tokens (700 target/900 hard gate), Huge quiet growth 0.2849%, fragmented anchor 5,449, chaotic anchors 5,668/15,809 at 64K/256K. Exact provider tokenization was not rerun; the 5,280-byte system prompt hash is unchanged. The 100/1,000/5,000-row history fixture retains 65 rows; the JSON ledger records each cleanup/watch/inspection/runtime measurement. These are deterministic token/storage fixtures, not thousands of provider queries or live-native measurements.

### Exact script inventory

- `scripts/active_scope_collector_benchmark.py`
- `scripts/attention_communication_contract_test.py`
- `scripts/campaign_journal_test.py`
- `scripts/counterfactual_choice_contract_test.py`
- `scripts/counterfactual_contract_test.py`
- `scripts/cross_publication_episode_test.py`
- `scripts/derived_lifecycle_test.py`
- `scripts/event_time_episode_test.py`
- `scripts/fair_play_world_test.py`
- `scripts/geographic_semantics_contract_test.py`
- `scripts/graphiti_projection_test.py`
- `scripts/graphiti_worker_contract_test.py`
- `scripts/inspection_action_lifetime_test.py`
- `scripts/journal_idempotency_index_test.py`
- `scripts/journal_internal_crash_test.py`
- `scripts/milestone_contract_test.py`
- `scripts/milestone_threshold_transition_test.py`
- `scripts/native_event_time_contract_test.py`
- `scripts/native_observation_contract_test.py`
- `scripts/observation_batch_recovery_test.py`
- `scripts/plan_dependency_publication_test.py`
- `scripts/plan_dependency_transition_test.py`
- `scripts/plan_health_contract_test.py`
- `scripts/provider_schema_budget_test.py`
- `scripts/publication_transaction_test.py`
- `scripts/publication_visibility_test.py`
- `scripts/query_history_scaling_test.py`
- `scripts/query_pin_consumers_test.py`
- `scripts/rollback_world_contract_test.py`
- `scripts/runtime_context_contract_test.py`
- `scripts/semantic_consumer_contract_test.py`
- `scripts/semantic_ref_matrix_test.py`
- `scripts/semantic_ref_recovery_test.py`
- `scripts/sovereign_geography_acceptance_test.py`
- `scripts/sovereign_hardening_contract_test.py`
- `scripts/spatial_scope_contract_test.py`
- `scripts/specialist_contract_test.py`
- `scripts/strategic_world_fixtures_test.py`
- `scripts/transient_episode_publication_test.py`
- `scripts/world_context_benchmark.py`
- `scripts/world_model_contract_test.py`

### Evidence boundaries and remaining limitations

- Deterministic: real-file journal crash/recovery, SQLite and managed runtime/query/attention/rollback/fairness/context contracts.
- Source-derived reviewer probes: source assertions and direct consumer/coalescer fixtures; these do not establish native gameplay equivalence.
- Native-shaped production collector: concurrent visibility, episode/gap/staging/retry, batched publication and five active-scope repetitions.
- Compiled adapter: production event adapter compiled on the host with controlled native-shaped input; native bridge cross-build unchanged.
- Running-game native: no new game/LAN session, native mechanic comparison or actual UI-thread timing run. Earlier native/.NET/provider acceptance remains historical. Native Pact contact/reopening-map refresh still lacks a clean controlled comparison; no continuous allied live vision is implied.
- Private episode checkpoints cannot reconstruct unavailable historical continuity. A feed gap or unproved snapshot association qualifies or splits identity; it never guesses opponent identity. Inspection validation follows the complete query contract without renewing the time of the explicit inspection; unrelated action or material churn is not an expiry condition. Full psych/commerce/research/climate simulation remains outside counterfactual coverage.

No new top-level tool, strategist, ranking layer, doctrine, hidden-state channel or merge. The resulting branch is ready for the requested independent final review, not marked as having passed an external review that has not occurred.
