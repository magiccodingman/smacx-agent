# AI - 7 semantic clarity follow-up

The early review found two interface ambiguities independent of latency.

## Plan validation checkpoint

Reference syntax rejection retains `invalid_plan_reference` for compatibility,
but now identifies the field and array index for target_refs, dependencies and
linked_commitments. Guidance distinguishes reference strings from prose conditions
and preserves unrelated valid bindings. MCP schema prose explicitly describes
dependencies/linked_commitments. Nothing is automatically rewritten or retried.
Existing guard and journal authority remain unchanged.

Controlled writer tests reproduce a prose dependency, prove no cognition change
on rejection, then persist the corrected plan with its participant intact. They
also cover target/commitment reference errors and MCP delivery of field guidance.

## Repair/defense scope checkpoint

The shared provider serializer labels global repair-rule objects as repair-only,
including restored/cached/nested objects and anchor summaries. Rule values and
current/stale/unknown evidence are preserved. Global configuration does not prove
ownership of the required facility or confer a combat defense bonus. Logistics
labels the same scope and runtime defense evidence states that garrison counts
and response ETA do not establish combat odds. No combat or repair formula changes.

Tests cover nested/restored/inspected/anchor objects, idempotence and immutable
evidence across epistemic states, plus existing geographic repair applicability
and bounded runtime-context contracts. Live interpretation remains to be reviewed
after recovery; a label cannot guarantee correct model reasoning.

## Maintenance

Two ordinary park attempts aborted safely (state changed before AI freeze, then
control request unavailable). The supported operator pause contained both seats:
zero sovereigns and frozen workers. A paired, save-verified turn-5 checkpoint is
available. This follow-up uses verified recovery, not frozen RAM as a checkpoint.
No database rebuild or native contract change is required.
