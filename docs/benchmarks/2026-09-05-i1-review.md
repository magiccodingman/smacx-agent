# I1 owned transient lifecycle acceptance — 2026-09-05

Root cause: production translated its authoritative owned birth independently, while temporal episode assignment knew ownership only from endpoint snapshots. The semantic translator then classified every episode assignment as foreign. A produced-and-destroyed unit absent from both endpoints therefore split into an owned birth and foreign destruction.

The existing private, perspective/timeline/epoch checkpoint now carries owned birth proof keyed by native handle, with the birth occurrence sequence. The same sequence assignments drive production and owned movement/damage/destruction translation. Destruction or loss ends the open proof; a same-segment loss/destruction pair retains its identity for the closing event. Gaps/resets clear proof, namespace changes reject it, and explicit hostile/allied/neutral evidence terminates it. A subsequent handle reuse without new owned production evidence gets no inherited owned proof. No global handle registry or new semantic-reference system was added.

`owned_transient_lifecycle_test.py` passes 20 cases: direct, movement, damage, and loss-before-destruction × single/split publication × ordinary/retry recovery, plus timeline, epoch, gap and explicit ownership-transition boundaries. Every collector case also verifies foreign handle reuse after termination. The endpoint snapshots contain no unit 777. Canonical journal semantic batches and normal temporal history each contain exactly one production and one owned destruction referencing `own-unit-777`, with zero fabricated contact destruction before the deliberate reuse. The strengthened test passes using production code packaged in the rebuilt image, with only test scripts mounted.

All 11 directly affected scripts pass; see [machine-readable evidence](results/2026-09-05-i1-review.json). This includes the prior 17 episode/gap adversaries, 22 publication crash cases, publication visibility, batched recovery and internal journal crash tests. Native bridge code is unchanged; no unrelated native suite or live gameplay equivalence is claimed.

The local stack was rebuilt with the current source and new application volumes after the user explicitly authorized a fresh database. Previous volumes remain intact. Control and specialist source hashes match the repository; portal HTTP returns 200. This is deployment verification, not a played-game acceptance claim.

Future schema changes must provide versioned, tested forward migrations preserving existing application data. The fresh database is the explicitly authorized prerelease cutover for this deployment, not the policy for subsequent releases. Implementing a migration framework is deferred from this strictly I1-only correctness patch; the existing schema guard is not weakened.
