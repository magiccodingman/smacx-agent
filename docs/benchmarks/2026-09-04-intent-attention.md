# Sovereign checkpoint 3: intent and attention

Status: **accepted**. Parent checkpoint:
`b6195b30f792153d6ea5bee8816f30dfa6b0bdd1`.
The [sanitized results](2026-09-04-intent-attention-results.json) record the
acceptance assertions and measurements. Checkpoints 4 and 5 remain outstanding.

## Gameplay chain established

The isolated native MCP campaign exercised the existing 15-tool surface to
discover bases, create a private base-radius scope, write a journaled plan and
express production milestones. The actual native production routine completed
two Scouts, a Recreation Commons and the Weather Paradigm. The two new unit
identities and corresponding milestone readiness were verified through managed
queries. A subsequent refused project attempt emitted an interruption; a fresh
watch did not acquire a fabricated completion.

The managed native-action run was followed by resumed intent/recovery acceptance
on the same isolated installation. The resumption corrected test-helper handling
of choice meanings and intentional identical-watch merging. The committed
`control_worker_mcp_live_test.py` and `intent_checkpoint_live_test.py` contain
the resulting checks for a complete repeat run.

The project information window closes through a guarded semantic choice using
the game's native modal-release handler. Its active-window identity and project
selection participate in the revision guard. This repairs the actual managed
path exposed by native project completion. Public project owner references also
serialize byte-sized faction IDs as integers, avoiding invalid JSON.

Two plans written through `smac_memory_update` reserved different currently
offered production items in overlapping windows. `smac_cognition` reported the
conflict and an explicitly assigned stationary unit. Runtime attention delivered
the milestone, production and interruption events through nine acknowledged
batches. The trusted response hook was simulated; no provider inference is
claimed.

Managed checkpoint/park/recover preserved completed-unit identities, journaled
reservations, their conflict and the stationary assignment. Old-timeline watches
were discarded. The isolated worker was then parked successfully. Rollback
fixtures additionally cover future scopes/milestones, journal authority and
ephemeral-state removal.

## Bounded semantic contracts

| Layer | Evidence |
| --- | --- |
| Spatial scopes | Managed dispatch; proximity, physical/geographic source, corridor, native-table 21-square radius and bounded union; private membership; observed hostile crossings; stale qualification; restart, expiry and dependency invalidation |
| Milestones | All/threshold evaluation; current fields and facilities; distinct native completion receipts; destroyed requirements retained; SQL-only plans rejected; plan revision expires the old watch; durable transition delivery |
| Production | Native owned-only fixed-size hooks; completion distinct from queue advance/exhaustion and repeat/fallback; project interruption; stable birth identity retained even when reconciliation follows destruction |
| Plan health | Journal-authoritative explicit assignments, timed unit/base/credit reservations, qualified dependency changes, incomplete-intent reporting; overlapping broad regions do not manufacture location conflicts |
| Runtime | Existing total context budgets retained; participant/event focus promotion; shortened attention preserves milestone state and counts; routine mineral accumulation does not create base alarms |
| Base discovery | Compact pages retain a discoverable base reference by shortening auxiliary response/support lists; the final serialized estimate remains within the declared ceiling |

These fixtures establish deterministic adapter behavior. Comparing the 21
offsets with native `path.h` does not establish a yield prediction; those native
comparisons belong to checkpoint 4. No strategic weighting or automatic action
selection was added.

## Measurements and validation

| Check | Result |
| --- | --- |
| .NET | 63 passed, 0 failed |
| Native cross-build | 38/38 build steps completed |
| Managed tools / world schema | 15 / 492 conservative tokens |
| 65,536-square scope | 99-token public descriptor; 317-byte stored definition |
| Scope creation / inspection at that size | 2,732 / 2,267 ms |
| Quiet 100× tile-count increase | Descriptor 98 → 99 tokens |
| Runtime fixture, 64K / 256K | 4,742 / 8,112 estimated tokens |
| Chaotic 256K runtime fixture | Approximately 25.6K total; approximately 15.9K anchor |
| 512-base, 32-site, 21-square native stress | 240.354 ms wall; 251.149 ms maximum probe gap; under the unchanged 500 ms law and 256 KB receipt limit |
| Mature managed-memory reads | All bounded; maximum measured read 1,907 tokens |

World, observation, fair-play differential, journal, rollback, runtime, opaque
choice, semantic-progress and attention/communication regressions pass alongside
the new scope, milestone and plan-health contracts. Machine-readable results
retain exact fixture measurements. Historical Qwen tokenizer and prefix-cache
measurements were not rerun or relabeled as new evidence.
