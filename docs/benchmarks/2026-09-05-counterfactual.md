# Sovereign checkpoint 4: counterfactual assistance

Status: **accepted within the explicit prediction limits below**. Parent checkpoint:
`8f732139409aa5807d4bc2066b9fee6fc0d37825`.

## Controlled native comparisons established

These comparisons use isolated test inputs, ordinary managed opaque choices for
sovereign actions, and observations of the running native game for effects.
They do not establish a universal future simulator.

| Calculation | Comparison already passed | Remaining qualification |
| --- | --- | --- |
| Site founding | Predicted center yield matched managed founding | Recycling Tanks delta also matched managed build and actual native completion; other variants remain conditional |
| Site variants | Read-state restoration; hidden mirror and foreign worker changes did not affect fair-play output | Other facility/technology/improvement variants are conditional native-helper hypotheses, not individually executed outcomes |
| Social Engineering | Ratings, switching charge, support burden and mineral-stock rescaling matched native effects | Full psych, commerce and research recomputation are not implemented; LAN regression remains an integrated gate |
| Terraforming | Cooperative farm completion matched standalone and affected owned-base yields | Climate and secondary harvest effects remain unmodeled; continuation of cooperating workers is conditional |
| Production and upgrade | Positive retool loss, hurry charge/progress and upgrade charge matched execution | Colony size decisions and unavailable surplus yield no promised completion time |
| Production plus travel | Focused comparison matched twelve actual upkeeps; consolidated comparison matched four after Recycling Tanks, followed by one native movement phase | Fixed-surplus native upkeeps without mineral injection, not full campaign turns |
| Action collateral | Rehome/disband support changes and explicit journal links matched managed effects | Local police capacity does not establish actual drone suppression or riot state |

Exploratory runs exposed and repaired the public `native_`-prefix serialization
collision, the `own_unit_ref` relationship adapter, and single-player Social
Engineering applying selections without local effects. Native dialog disassembly
(`0x5B566B`–`0x5B5682`) establishes the conditional `social_set` call; the managed
action now follows it while multiplayer retains network application.

Recovery exposed a first-query reconciliation race: journal plans could be
visible before their restored world participants. Plan health now synchronously
refreshes the world and propagates refresh failure. The adapter regression
passes; the corrected live first-query recovery continuation also passed.

The paged owned-unit feed now includes boarding identity and aircraft-carrier
capacity reservations. Deployment uses the explicit transport schedule for a
boarded passenger. Stale boarding or transport inputs cannot produce a current
ETA; stale/reserved carrier capacity cannot supply a refuelling destination.
The updated managed native boarding comparison passed. The subsequent Python-only
boarded-route correction is covered by deterministic adapter tests; final
integrated acceptance will use the rebuilt image containing that correction.

## Deterministic and interface evidence

- Joint nutrient/mineral/energy alternatives match exhaustive distinct-worker
  assignments in **45 cases**. No independent resource maxima or strategic
  weights are substituted for feasible assignments.
- Stale centers, duplicate workers, invalid population requests and open-ended
  parameters are rejected or explicitly unavailable.
- World receipts are bound to the action revision. Opaque preview fixtures
  reject wrong kinds, seats, revisions, expired and consumed decisions, and
  demonstrate repeated previews do not consume or modify the executable choice.
- Aircraft fuel, transport composition, nominated production, stale movement,
  boarding identity and reserved-carrier fixtures establish adapter/routing
  behavior. They are not additional native flight/arrival comparisons.
- World-model, geographic, movement, opaque-choice, rollback, plan-health and
  semantic-progress regressions have passed. All eleven scripts passed in the latest rerun, including the transport changes.
- Fair-play honeytoken boundaries passed. The host specialist-supervisor
  contract passed process isolation, budgets, cancellation and recovery.
- .NET: **63 passed** (7 application tests and 56 harness tests).
- Native worker image: all **38 cross-build steps** completed after the owned
  transport observation changes.

## Measurements

| Probe | Result | Evidence class |
| --- | --- | --- |
| Provider surface | 15 tools; world schema 1,855 bytes / 619 conservative tokens | Current schema measurement |
| System prompt | 1,117 exact Qwen3.8-27B tokens; SHA-256 `d185f0f91fb92ff88fec7b8c16cd1ede072c3baddaa6dada7a4335edb88708d7` | Live tokenizer; no new inference/prefix-cache claim |
| Dense site standard/deep | 2,034 / 2,213 estimated tokens, approximately 18–21 ms | Synthetic 20-square, population-four query fixture |
| Dense site compact | Explicit 63-token size error | Whole-response bound; no empty success |
| Four-site native economy | 247.638 ms wall; 233.875 ms maximum probe gap; 52,322 bytes | 511 temporary base input rows, leaving one native hypothesis slot |
| Existing 32-site native receipt | 243.838 ms wall; 249.663 ms maximum probe gap; 170,523 bytes | 512 temporary base rows; both probes retain the 500 ms/256 KB gates |

## Acceptance and recovery

The R8 native managed sequence passed the mechanical comparisons, explicit
intent events and attention delivery, and both site responsiveness probes.
Its process then stopped because the new preview-recovery test attempted to
mint a choice after ending its request context. The guard correctly refused.
The test now opens and closes a dedicated request context; no runtime guard was
weakened. A continuation on the same preserved installation executed that
corrected recovery block and passed:

- Native completed-unit identities survived checkpoint/park/recover.
- The **first** plan-health query preserved active plans, their conflict and
  explicit stationary assignment.
- Old-timeline milestone watches and the old-session preview reference were
  rejected.
- The restored worker parked successfully.

This is a managed sequence plus focused recovery continuation, **not a claim
that the original full process exited successfully**. Checkpoint 5 will rerun
the corrected full process with the latest Python transport calculation image.
The arithmetic and adapter tests cover that final Python-only correction.

[Sanitized acceptance evidence](2026-09-05-counterfactual.json) records measured
results, evidence classes and reproducible commands. Raw exploratory logs remain
under ignored `work/pr48`; failed installations are removed after preserving
logs. The coverage matrix is updated before proceeding to checkpoint 5.

Unknown aircraft fuel, complete climate simulation, and complete base psych,
commerce and research recomputation remain explicit limits. No unsupported
prediction is labeled exact.

Checkpoint 5 and final integrated acceptance remain outstanding. Do not merge
PR #48.
