# Operational geography acceptance

This repair adds bounded semantic comparisons to the existing tool surface. It
changes neither game rules nor native action guards. It does not claim that a
model uses the evidence well or that a new campaign has succeeded.

Checkpoint 1 source checks:
- `operational_geography_test.py`: 3,000-node chain, iterative articulation,
  separated side sizes, wraparound alternate, remote fog qualification, bounded
  two-tile cuts, stale economic rates, current queue item, overlap union bounds,
  stationary endpoint interpretation and epoch isolation. Chain calculation with
  two detailed results: approximately 0.074 seconds on this host.
- `geographic_semantics_contract_test.py`: native-shaped observations through
  projection/store/WorldService to frontier comparison and physical-area detail;
  existing geography, entitlements, repair and Huge-map contracts pass.
- `strategic_world_fixtures_test.py`: existing 20 strategic fixture groups pass.
- `runtime_context_contract_test.py`: existing 64K/256K pressure gates pass.
- `fair_play_world_test.py`: hidden honeytoken differential passes.

Evidence limits: wider passage enumeration samples adjacent pairs and is skipped
above 4,096 known passable nodes; cuts describe terrain topology, not guaranteed
military control or special-transit reachability. No unseen area/probability is
estimated. Assignment history is a bounded canonical journal window, not a new
source of mechanical truth. Native comparison and behavioral validation are not
claimed by these synthetic/adapter checks. No paid provider or campaign used.

Final installed-image/provider/deployment acceptance: pending.

Checkpoint 2 implementation: economic review uses native queue shape (entry zero
is current); order history is request-only and scoped to the current epoch and
committed observation. Runtime support advisories retain native authority. The
provider schema gate remains 15 tools and 842 conservative tokens for smac_world
(900 hard ceiling); no extra doctrine text or paid tokenization was used.

Checkpoint 2 validation completed:
- Container MCP environment: persistent order attention (including native-target
  to semantic-destination capture), Former automation, intent reconciliation,
  citizen managed delivery and fair-play contracts pass.
- Real Hermes image → local controlled HTTP provider: new operational exceptions
  survive the request-only context boundary in gameplay/communication episodes;
  existing initial/resume, semantic-GC, diagnostic and direct-tool assertions pass.
- WorldService checks additionally exercise intel geography and deep connector
  query delivery. No production provider requests were made.
- Original campaign read-back confirms parked turn 17/year 2117 and an available
  verified checkpoint. The campaign has not been resumed for this validation.
