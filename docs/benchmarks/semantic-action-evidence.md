# Semantic action evidence repair

## Checkpoint 1 — availability, lifecycle and destructive consequences

The parked turn-11 trace showed an Alien Artifact at its owned base with no
movement remaining. The native catalog returned only disband, and its wording
called the unit "spent". The sovereign incorrectly promoted that temporary
action state into proof that the Artifact benefit had already been consumed.

The native unit catalog now reports its readiness reason, movement scale,
positive Artifact role and an explicitly unknown strategic-completion state.
Managed decision and choice frames retain those facts and state what the closed
catalog does and does not prove. Disband wording now identifies permanent
removal, eligible recycling and the surviving uncertainty for both Artifacts
and ordinary units.

`decision_frame_test.py` proves that native-shaped readiness, movement and
lifecycle fields cross the managed adapter. The complete MinGW bridge build
passes in Docker (`smacx-astra-bridge-semantic-evidence:dev`). Installed
provider-delivery validation remains required before this checkpoint is called
deployed. No strategy is prescribed and destructive execution remains behind
the existing exact choice and native confirmation.
